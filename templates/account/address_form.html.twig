{% extends 'base.html.twig' %}

{% block title %}Ajouter une adresse - El cafexito{% endblock %}

{% block content %}

    <h1 class="mt-5 text-center">Gérer mes adresse</h1>


    <hr>

    {{ form_start(form) }}
        <div class="row">
            <div class="col-md-6">{{form_row(form.name)}}</div>
            <div class="col-md-6">{{form_row(form.company)}}</div>
            <div class="col-md-6">{{form_row(form.firstname)}}</div>
            <div class="col-md-6">{{form_row(form.lastname)}}</div>
            <div class="col-md-6">{{form_row(form.address)}}</div>
            <div class="col-md-3">{{form_row(form.postal)}}</div>
            <div class="col-md-3">{{form_row(form.city)}}</div>
            <div class="col-md-6">{{form_row(form.country)}}</div>
            <div class="col-md-6">{{form_row(form.phone)}}</div>
            <div  class="col-md-12 ">{{form_row(form.submit)}}</div>


        </div>


    {{ form_end(form) }}
       <a href="{{ path('app_account_address') }}" class="btn btn-danger btn-sm">Retour</a>
{% endblock %}